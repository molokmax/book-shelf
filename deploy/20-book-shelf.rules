polkit.addRule(function(action, subject) {
    if (action.id == "org.freedesktop.systemd1.manage-units" && 
        action.lookup("unit") == "book-shelf.service" &&
        subject.isInGroup("molokmax")) {
        return polkit.Result.YES;
    }
});