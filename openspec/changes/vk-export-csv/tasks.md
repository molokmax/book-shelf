## 1. Setup

- [x] 1.1 Добавить кнопку «Экспорт CSV» в обработчик `src/vk_bot/handlers/help.py` (инлайн‑клавиатура).
- [x] 1.2 Реализовать метод экспорта в новом файле `src/utils/export.py` (функция `export_to_csv`).
- [x] 1.3 Создать обработчик `export_csv` (в `src/vk_bot/handlers/export.py`) и зарегистрировать его в боте.
- [x] 1.4 Добавить импорт и привязку нового обработчика в `src/vk_bot/bot.py`.

## 2. CSV Generation

- [x] 2.1 Реализовать функцию `export_to_csv(books: List[Book]) -> Path`:
    - Использовать модуль `csv`.
    - Писать заголовки `id,title,author,tags,pages,current_page,status,created_at`.
    - Сохранять файл во временный каталог (`tempfile.TemporaryDirectory`).
- [x] 2.2 В обработчике `export_csv` вызвать `export_to_csv`, затем отправить файл пользователю через VK API и удалить временный файл.

## 3. Testing

- [ ] 3.1 Написать unit‑тест для `export_to_csv`, проверяющий правильный набор столбцов и порядок записей.
- [ ] 3.2 Добавить интеграционный тест обработчика `export_csv` с мок‑объектом VK API, проверяющий отправку файла.

## 4. Documentation & Cleanup

- [ ] 4.1 Обновить файл `README.md` или раздел помощи бота, описав новую кнопку и её назначение.
- [ ] 4.2 Убедиться, что линтер (`flake8`) проходит без ошибок.